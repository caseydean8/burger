<h1>Burgers!</h1>

<ul>
  {{#each burgers}}
    <li>
      <p>
        {{this.id}}. {{this.burger_name}}

        {{!-- <button data-burgerid="{{this.id}}" class="delburg">Delete</button> --}}
      </p>
    </li>
  {{/each}}
</ul>